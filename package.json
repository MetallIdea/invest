{
  "name": "invest",
  "version": "0.1.0",
  "private": true,
  "scripts": {
     "scheduler": "npm start --workspace=scheduler",
     "server": "npm start --workspace=server"
  },
  "workspaces": ["packages/common", "packages/scheduler", "packages/server"]
}
