{
  "name": "common",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "tsx main.ts",
    "generate": "npx drizzle-kit generate",
    "generate:empty": "npx drizzle-kit generate --custom",
    "migrate": "npx drizzle-kit migrate",
    "update": "npx drizzle-kit push",
    "lint": "eslint"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.45.1",
    "drizzle-kit": "^0.31.8",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^9",
    "eslint-config-next": "16.0.5",
    "typescript": "^5"
  }
}
